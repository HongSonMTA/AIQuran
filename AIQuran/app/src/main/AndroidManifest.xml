<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aiquran.aiquran">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ScrollActivity" />
        <activity android:name=".activities.BookMarkActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.ResultSearchActivity" />
        <activity android:name=".activities.PagingActivity" />
        <activity android:name=".activities.SettingActivity" />
        <activity android:name=".activities.SettingTafsirTranslationActivity" />
        <activity android:name=".activities.AudioActivity" />
        <activity android:name=".activities.ActivityDownloadAudio" />
    </application>

</manifest>